# Build artifacts
/bin/
/dist/
karya

# Go
*.test
*.out
coverage.*

# Editor / OS
.DS_Store
*.swp

# The vendored Neovim config (internal/assets/nvim/) IS committed: go:embed needs
# it present in every checkout (CI, GoReleaser). Refresh it with `make sync-nvim`.
